# Core app
streamlit>=1.60.0,<2.0.0
pydantic>=2.9.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# AI providers. Both pins matter: this app targets OpenAI's Responses API
# (client.responses.parse) and Gemini's Interactions API (client.interactions.create),
# which are recent additions to each SDK.
openai>=3.0.0,<4.0.0
google-genai>=2.3.0,<3.0.0

# Static analysis
pylint>=3.3.0,<5.0.0
flake8>=7.0.0,<8.0.0

# Dev / test only
pytest>=8.0.0,<9.0.0
